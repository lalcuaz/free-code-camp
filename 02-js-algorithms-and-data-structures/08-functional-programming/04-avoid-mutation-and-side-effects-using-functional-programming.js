// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line
  let incrementedValue = fixedValue + 1;
  return incrementedValue;

  // Only change code above this line
}
